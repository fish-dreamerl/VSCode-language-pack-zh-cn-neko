# 猫娘中文语言包 · 安装教程

> 前置要求：VS Code 版本 **≥ 1.131**（`engines.vscode: ^1.131.0`）。

## 一、安装语言包（三选一）

### 方法 1：VSIX 文件安装（推荐）
1. 打开 VS Code，按 `Ctrl+Shift+P` 打开命令面板
2. 输入并选择 `Extensions: Install from VSIX...`
3. 选择打包产物：`vscode-language-pack-zh-cn-neko-1.131.4.vsix`
4. 等待底部提示安装完成。

### 方法 2：命令行安装
```powershell
code --install-extension "vscode-language-pack-zh-cn-neko-1.131.4.vsix" --force
```

### 方法 3：手动放置文件
把 VSIX 解压出的目录放入扩展目录（Windows）：
```
%USERPROFILE%\.vscode\extensions\
```

## 二、切换显示语言
1. 按 `Ctrl+Shift+P`，运行 **`Configure Display Language`**。
2. 选择 **猫娘中文（Neko Chinese）**（languageId 为 `zh-cn-nekko`）。
3. **完全退出并重新打开** VS Code —— 是彻底退出进程，不是仅关闭窗口。

> 若列表未出现语言包：确认已安装后再试；或在
> `%USERPROFILE%\.vscode\argv.json` 中写入 `"locale": "zh-cn-nekko"`，然后重启。

## 三、安装到另一台电脑
把 `.vsix` 拷贝到目标机器，重复「方法 1 或 2」即可。该机 VS Code 版本需 ≥ 1.131。

## 四、恢复中文原版
在 `Configure Display Language` 中改回 **简体中文**（`zh-cn`）并重启。
本语言包不会覆盖或删除官方简体中文语言包。